apiVersion: v1
kind: Service
metadata:
  name: postgresql-service 
  namespace: backend
  labels:
    app: postgresql
spec:
  type: ClusterIP
  selector:
    app: postgresql 
  ports:
    - protocol: TCP
      port: 5432 
      targetPort: 5432 
 
 